package com.guessNumber.GetRandomNumber;

 public class  GetRandomNumber {
	 public inite(){
  int[]randomnum=new int[4];
  for(int i=0;i<4;i++){
	  randomnum[i]=(int)Math.round(Math.random()*10);
  }
  for(int j=0;j<random.length;j++){
	  System.out.println(randomnum[i]+" ");
	}
}
}